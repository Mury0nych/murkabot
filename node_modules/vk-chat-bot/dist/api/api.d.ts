import Stats from '../extra/stats';
export default class API {
    private vkToken;
    private stats;
    private queue;
    private isQueueProcessing;
    constructor(vkToken: string, stats: Stats);
    scheduleCall(method: string, params: {
        [key: string]: string;
    }): Promise<any>;
    call(method: string, params: {
        [key: string]: string;
    }): Promise<any>;
    send(pid: string | number, message: string, attachment: string, keyboard: string): Promise<void>;
    private checkPermissions;
    private processQueue;
}
