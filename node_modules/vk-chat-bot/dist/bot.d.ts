import Core from './core';
export default class Bot {
    private core;
    private groupId;
    private confirmationToken;
    private secret;
    private port;
    constructor(core: Core, groupId: string, confirmationToken: string, secret: string, port: number);
    start(): void;
}
