import IScene from './scenes/scene';
import { IContext } from './types';
import { ISceneManagerOptions } from './scene-manager.types';
export default class SceneManager {
    private repository;
    constructor(rawOptions?: ISceneManagerOptions | IScene[]);
    /**
     * Adds a scene to the shared list
     */
    addScene(scene: IScene): this;
    /**
     * Returns the middleware for embedding
     */
    readonly middleware: (context: IContext, next: Function) => any;
    /**
     * Returns the middleware for intercept
     */
    readonly middlewareIntercept: (context: IContext, next: Function) => any;
}
